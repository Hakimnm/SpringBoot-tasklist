<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.programmingtechie.tasklist.repository.TaskRepository">

    <resultMap id="TaskResultMap" type="com.programmingtechie.tasklist.domain.task.Task"
               autoMapping="false">
        <id property="id" column="task_id"/>
        <result property="title" column="task_title"/>
        <result property="description" column="task_description"/>
        <result column="task_expiration_date" property="expirationDate"/>
        <result property="status" column="task_status"/>
    </resultMap>
    
    <select id="findById" resultMap="TaskResultMap">
        SELECT t.id task_id
             ,t.title task_title
             ,t.description task_description
             ,t.status task_status
             ,t.expiration_date task_expiration_date
        FROM tasklist.tasks t where t.id=#{id}
    </select>

    <select id="findAllByUserId" resultMap="TaskResultMap">
        SELECT t.id task_id
             ,t.title task_title
             ,t.description task_description
             ,t.status task_status
             ,t.expiration_date task_expiration_date
        FROM tasklist.tasks t
                 join tasklist.users_task ut on t.id = ut.task_id
        where ut.user_id=#{userId}
    </select>

    <insert id="assignToUserById">
        INSERT INTO tasklist.users_task(task_id,user_id)VALUES (#{task_id}, #{user_id})
    </insert>
    
    <update id="update">
        UPDATE tasklist.tasks
        SET title=#{title},description=#{description},status=#{status}?,expiration_date=#{expiration_date} where id=#{id}
    </update>

    <insert id="create" keyProperty="id" keyColumn="id" useGeneratedKeys="true">
        INSERT INTO tasklist.tasks(title,description,status,expiration_date)
        VALUES(#{title},#{description},#{status},#{expiration_date})
    </insert>

  <delete id="delete">
      DELETE FROM tasklist.tasks where id=#{id}
  </delete>

</mapper>
