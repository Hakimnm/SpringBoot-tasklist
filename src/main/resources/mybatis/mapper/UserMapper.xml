<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.programmingtechie.tasklist.repository.UserRepository">

    <resultMap id="UserResultMap" type="com.programmingtechie.tasklist.domain.user.User"
               autoMapping="false">
            <id property="id" column="user_id"/>
            <result property="name" column="user_name"/>
            <result property="username" column="user_username"/>
            <result property="password" column="user_password"/>
            <collection property="roles"
                        ofType="com.programmingtechie.tasklist.domain.user.Role"
                        autoMapping="false">
                    <result property="name" column="user_role_role"/>
            </collection>
       <collection property="tasks" ofType="com.programmingtechie.tasklist.domain.task.Task"
                   autoMapping="false"/>
    </resultMap>

    <select id="findByUsername" resultMap="UserResultMap">
        SELECT
            u.id as user_id,
            u.name as user_name,
            u.username as user_username,
            u.password as user_password,
            ur.role as user_role_role,
            t.id as task_id,
            t.title as task_title,
            t.description as task_description,
            t.expiration_date as task_expiration_date,
            t.status as task_status
        FROM tasklist.users u
                 left join tasklist.users_roles ur on u.id = ur.user_id
                 left join tasklist.users_task ut on u.id = ut.user_id
                 left join tasklist.tasks t on t.id = ut.task_id
        where u.username=#{username}
    </select>

    <select id="findById" resultMap="UserResultMap">
        SELECT
            u.id as user_id,
            u.name as user_name,
            u.username as user_username,
            u.password as user_password,
            ur.role as user_role_role,
            t.id as task_id,
            t.title as task_title,
            t.description as task_description,
            t.expiration_date as task_expiration_date,
            t.status as task_status
        FROM tasklist.users u
                 left join tasklist.users_roles ur on u.id = ur.user_id
                 left join tasklist.users_task ut on u.id = ut.user_id
                 left join tasklist.tasks t on t.id = ut.task_id
        where u.id=#{id}
    </select>

    <select id="isTaskOwner" resultType="boolean">
        SELECT exists(
            SELECT 1 FROM tasklist.users_task WHERE user_id=#{user_id} AND task_id=#{task_id})
    </select>

    <insert id="create" keyProperty="id" keyColumn="id" useGeneratedKeys="true">
        INSERT INTO tasklist.users(name, username, password) VALUES (#{name},#{username},#{password})
    </insert>

    <insert id="insertUserRole">
        INSERT INTO tasklist.users_roles (user_id, role) VALUES (#{user_id},#{role})
    </insert>

    <update id="update">
        UPDATE tasklist.users
        SET name=#{name},
            username=#{username},
            password=#{password},
            WHERE id=#{id}
    </update>

    <delete id="delete">
        DELETE FROM tasklist.users WHERE id=#{id}
    </delete>
    
</mapper>
